# Autoplay Tools

This directory contains command-line tools for headless game runs.

The first tool, `mk_headless_run`, loads the East Mosul block fixture and advances it for a fixed number of deterministic ticks. Future cycles should extend this into AI-vs-AI autoplay with scenario selection, controller assignment, replay output, and batch balance summaries.
